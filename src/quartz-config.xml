<?xml version="1.0" encoding="UTF-8"?>
<job-scheduling-data
	xmlns="http://www.quartz-scheduler.org/xml/JobSchedulingData"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.quartz-scheduler.org/xml/JobSchedulingData 
	http://www.quartz-scheduler.org/xml/job_scheduling_data_1_8.xsd"
	version="1.8">

<pre-processing-commands>
    <delete-jobs-in-group>AGroupA</delete-jobs-in-group>
    <delete-triggers-in-group>TRIGGER_GROUP</delete-triggers-in-group>
</pre-processing-commands>
	<schedule>
		<job>
			<name>AJobA</name>
			<group>AGroupA</group>
			<description>Print a welcome message</description>
			<job-class>com.test.quartz.SchedulerJob</job-class>
		</job>

		<trigger>
			<cron>
				<name>dummyTriggerName</name>
				<group>TRIGGER_GROUP</group>
				<job-name>AJobA</job-name>
				<job-group>AGroupA</job-group>
				<!-- It will run every 5 seconds -->
				<cron-expression>0 * * * * ?</cron-expression>
			</cron>
		</trigger>
	</schedule>
</job-scheduling-data>